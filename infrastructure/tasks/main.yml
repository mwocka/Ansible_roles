- name: Install docker-compose python package
  pip:
    name: 
    - docker
    - docker-compose

- name: Git checkout
  git:
    repo: "{{ git_repository }}"
    dest: "{{ deployment_path }}"
    version: "{{ git_branch }}"
